name: 018 BasicsCustomCompositeAction CI/CD
on:
    workflow_dispatch:

jobs:
    print-hello-world:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout
              uses: actions/checkout@v4.2.2

            - name: Print Hello World
              uses: ./.github/actions/printHelloWorld
