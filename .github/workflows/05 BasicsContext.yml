name: 05 Basics Context CI/CD
run-name: ${{ github.actor }} explaining the context
on:
  workflow_dispatch:

jobs:
  print-hello-world:
    runs-on: ubuntu-latest
    steps:
      - name: Print Context Data
        run: echo "${{ toJSON(env) }}"
#        run: echo "${{ github.actor }}"
