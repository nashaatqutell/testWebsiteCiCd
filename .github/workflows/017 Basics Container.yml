name: 017 Basics Container CI/CD
on:
    workflow_dispatch:
jobs:
    print-hello-world:
        runs-on: ubuntu-latest
        container:
          image: php:7.4
        steps:
            - name: print version php
              run: php -v

            - name: install redis
              run: apt-get update && apt-get install -y redis

    job2:
        runs-on: ubuntu-latest
        services:
          redis:
          image: redis:latest
        steps:
            - name: install redis
              run: apt-get update && apt-get install -y redis

            - name: access redis
              run: redis-cli ping
